import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.SharpBrainer.app',
  appName: 'Sharp Brainer',
  webDir: 'dist/Sharp-Brainer/browser'
};

export default config;